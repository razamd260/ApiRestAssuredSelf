package Remember;

import io.restassured.specification.QueryableRequestSpecification;
import io.restassured.specification.SpecificationQuerier;

public class JustRead {
/*
 * #QueryableRequestSpecification requestquery=SpecificationQuerier.query(req); 
 * first Response response=get();then these method will work
 * 1. response.getbody(); 
 * 2. getHeaders(); and use For Each Loop
 * #Response response=req.response();    
 * 2. response.then();
 * 3. response.jsonpath();
 * 4. response.Headers();
 * 
 * #JsonPath json=response.jsonpath();
 * 1. json.get("data[0].first_name");
 * 
 * #QuerableRequestSpecification queryrequest=SpecificationQuerier.query(request);
 * 1. queryrequest.geturi();
 * 2. queryrequest.getBaseuri();
 * 3. queryrequest.getBasepath();
 * 
 * first mandatory pass body like JasonObject obj=new JsonObject();
 *                                            obj.put("name","ali");
 *                                            obj.put("job","QA");
 *4. queryrequest.getbody();                                        
 *5. queryrequest.Headers();
 *note:-if you will you not pass object(Body) then then getbody()=null will come
 * 
 */
}
